import 'package:flutter/material.dart';

class ArabicRechercheScreen extends StatelessWidget {
  const ArabicRechercheScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lois Directs Screen'),
      ),
      body: const Center(
        child: Text(' Recherche Screen'),
      ),
    );
  }
}
